# 2026-02-19 — Major System Overhaul & Model Hierarchy

## Key Decisions

### Orchestrator Mode (NEW)
- Salah wants me (Axis) to act as **orchestrator only** — no direct task execution
- "Me and you just plan. The army of subagents builds."
- All tasks should be spawned as subagents, I focus on planning/strategy

### Model Hierarchy (NEW — Live)
| Layer | Model | Fallback | Cost |
|-------|-------|----------|------|
| Main session | Sonnet (claude-opus-4-6) | → Kimi (free) | $$$ |
| All subagents/cron | **Gemini Flash 2.0** | → Sonnet → Kimi | ¢¢¢ |

- Gemini API key added: `AIzaSyBt34aBeLiLQ7bINKdgNMgEv3ZROG-7aGA`
- Flash is default for all `sessions_spawn` and cron jobs
- 80% routine on Flash, complex work escalates to Sonnet automatically
- Estimated 60-70% cost reduction vs all-Sonnet

### Config Changes Applied
1. Primary model: `anthropic/claude-opus-4-6` (aliased "Sonnet")
2. Fallback: `nvidia/moonshotai/kimi-k2.5` (aliased "Kimi", free)
3. Added provider: `google/gemini-2.0-flash` (aliased "Flash")
4. Subagent default: Flash → Sonnet → Kimi fallback chain
5. Discord groupPolicy: `allowlist` (was `open` — security fix)

## System Health Work

### Gateway Issues
- Gateway on `2026.2.15`, update to `2026.2.17` blocked by npm permissions
- `sudo chown -R 503:20 "/Users/sms/.npm"` done but also need `.npm-global`:
  ```bash
  sudo chown -R 503:20 "/Users/sms/.npm-global"
  ```
- Gateway experiencing intermittent timeouts under load
- Morning briefing missed Wed Feb 18 (gateway was down at 8 AM)

### Cron Job Fixes
- **Cron Guardian** re-enabled (was disabled after 6 consecutive timeouts)
- **Stock Pivot Alert** timeout: 300s → 600s (was timing out on Kimi)
- **Cron Guardian** timeout: 180s → 360s
- **Portfolio Options Check** timeout bumped multiple times, now ~660s
- All jobs now running on Flash which should be much faster

### Browser Issues (Recurring)
- Browser (openclaw profile) keeps going down between Guardian runs
- Guardian auto-restarts it every 30 min
- Chrome extension relay needs tab attached for `chrome` profile
- `openclaw` profile works independently

## Cron Job Status (as of evening Feb 18)
- 14 enabled, 2 disabled (Mission Control Build, DTF Reservation)
- Stock Pivot Alert: had 4 consecutive errors, reset with new timeout
- Portfolio Options Check: 1 timeout error, monitoring
- All others: healthy

## Subagent Learnings
- Kimi subagents use `exec(openclaw cron ...)` instead of native tools — they don't understand tool availability
- Can't steer/kill subagents spawned before a gateway restart (session ID mismatch)
- Flash should fix the timeout issues since it's much faster than Kimi

## Tasks Completed
- Alaska waterproof jacket research → posted to #general (5 picks, REI Stormhenge recommended)
- Full LaunchAgent analysis → only git autocommit is a candidate, decided to keep everything in OpenClaw
- System health check and repair via subagent

## Still TODO
- [ ] Fix npm-global permissions: `sudo chown -R 503:20 "/Users/sms/.npm-global"`
- [ ] Retry gateway update to 2026.2.17
- [ ] Monitor Flash performance on cron jobs (first full cycle today)
- [ ] Browser keeps dying — investigate root cause beyond Guardian band-aid
